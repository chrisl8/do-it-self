# Dependencies (node_modules is ignored by Prettier by default; listed for clarity)
**/node_modules

# Built frontend bundle (generated by vite build)
web-admin/backend/public

# Lockfiles
**/package-lock.json

# Module clones / vendored code — not first-party, lives in their own repos
.modules

# Build artifacts
**/dist

# YAML is hand-maintained. compose.yaml files follow a deliberate house style
# (no blank lines, copious comments) that Prettier would fight — never format them.
**/*.yaml
**/*.yml
