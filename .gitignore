# IDE and Editor Files
.idea/

# Environment and Configuration Files
.env
tailscale.env
tailscale-state/
user-config.yaml
infisical/infisical-secrets.env

# GPU compose overrides (only present on systems with NVIDIA GPUs)
jellyfin/compose.override.yaml
mame/compose.override.yaml
obsidian/compose.override.yaml
retroarch/compose.override.yaml
secure-browser/compose.override.yaml

# Per-container personal config overrides (gitignored, defaults in config-defaults/)
*/config-personal/

# Generated config targets (built from config-defaults + config-personal at start)
the-lounge/config/
filez/copyparty.conf

# Application Configuration Files
forgejo/app.ini
obsidian-babel-livesync/docker.ini
scripts/healthcheck.conf
scripts/excluded_devices.conf
recon/config-links

# Web Server and Proxy Configuration
caddy/conf
caddy/site

# Application Data and Content Directories
tsidp/tsidp/
valheim/valheim-server-docker/
minecraft/docker-minecraft-bedrock-server/
nextcloud/html
searxng/etc

# Homepage Application
# config/ is generated by scripts/merge-homepage-config.js from
# config-defaults/ (in git) + config-personal/ (gitignored).
homepage/config/
homepage/config-personal/
# icons/ and images/ stay in the repo as empty dirs (via .gitkeep) but
# their contents are user-specific and not tracked. Exceptions: the
# small handful of default-shipped icons referenced by config-defaults/
# (e.g. do-it-self.svg used by the Web Admin tile in the Top group).
homepage/icons/*
!homepage/icons/.gitkeep
!homepage/icons/do-it-self.svg
homepage/images/*
!homepage/images/.gitkeep
# dashboard-icons is cloned by setup.sh from the upstream repo.
homepage/dashboard-icons/

# Docker Compose override files generated by
# scripts/regenerate-monitoring-mounts.js for homepage and beszel.
homepage/compose.override.yaml
beszel/compose.override.yaml

# Chat and Communication
the-lounge/config

# Documentation and Notes
upgrade_these_containers.txt
paste/paste.env

# Code Editor links
code/home
filez/copyparty.conf
stirling-pdf/configs
caddy/meshtastic
spacymote/spacymote
witchazzan/Witchazzan
meshtastic/meshtastic

cloudflared/config

# Node.js
actual-budget-sync/node_modules/
scripts/node_modules/
scripts/package-lock.json

# Personal post-startup hook (see system-cron-startup.sh)
scripts/post-startup-hook.sh