services:
  caddy:
    container_name: caddy
    user: 1000:1000
    image: caddy
    ports:
      - 80:80
    volumes:
      - ${DATA_ROOT:-/mnt/2000}/container-mounts/caddy/conf:/etc/caddy
      - ${DATA_ROOT:-/mnt/2000}/container-mounts/caddy/site:/srv
      - ${DATA_ROOT:-/mnt/2000}/container-mounts/caddy/data:/data
      - ${DATA_ROOT:-/mnt/2000}/container-mounts/caddy/config:/config
      - ${DATA_ROOT:-/mnt/2000}/container-mounts/caddy/logs:/var/log/caddy
      - /home/chrisl8/voidship_ephemeral/dist/admin:/srv/voidship-admin:ro
    networks:
      - caddy-net
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -qO - https://www.lofland.com/ | grep -q 'Ekpyrotic Frood Landing Page'",
        ]
      start_period: 120s
      start_interval: 5s
      interval: 5m
      timeout: 30s
      retries: 5
    restart: on-failure
networks:
  caddy-net:
    external: true
